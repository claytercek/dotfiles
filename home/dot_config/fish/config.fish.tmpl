if status is-interactive
    # Commands to run in interactive sessions can go here
end

set -gx EDITOR nvim

set -g fish_greeting

{{ if eq .chezmoi.os "darwin" -}}
{{ if contains "arm" (output "uname" "-p" | trim) -}}
/opt/homebrew/bin/brew shellenv | source
{{ else }}
/usr/local/bin/brew shellenv | source
{{ end }}
{{ end -}}

zoxide init fish | source

starship init fish | source

mise activate fish | source

alias cat="bat"
alias ls="eza"

export PATH="$HOME/bin:$HOME/go/bin:$HOME/.local/bin:$PATH"

export GIT_TERMINAL_PROMPT=1

# opencode
fish_add_path /Users/clay/.opencode/bin

# alias safe="safehouse"
# alias claude="safe claude --dangerously-skip-permissions"
# alias claude-work="CLAUDE_CONFIG_DIR=~/.claude-work SAFEHOUSE_ENV_PASS=CLAUDE_CONFIG_DIR SAFEHOUSE_ADD_DIRS=~/.claude-work claude"

alias claude-work="CLAUDE_CONFIG_DIR=~/.claude-work claude"
